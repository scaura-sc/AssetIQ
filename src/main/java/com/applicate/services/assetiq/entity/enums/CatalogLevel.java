package com.applicate.services.assetiq.entity.enums;

/** Hierarchy level within {@code aiq_asset_catalog} (CATEGORY -> TYPE -> MODEL). */
public enum CatalogLevel {
    CATEGORY,
    TYPE,
    MODEL
}
