package com.applicate.services.assetiq.entity.enums;

/** Where purity/condition were determined from — shared by purity_source and condition_source. */
public enum DetectionSource {
    MANUAL,
    AI_VISION
}
