package com.applicate.services.assetiq.entity.enums;

/** Discriminator for aiq_service_event_log — which of the type-specific column groups apply. */
public enum EventType {
    COMPLAINT,
    WORK_ORDER
}
